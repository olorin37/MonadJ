package Monad;

import java.util.function.Function;

/**
 * Created by olorin on 12.04.14.
 */
public interface Monad<T> {
   // public <T> Monad<T> unit(T val);

    //public <T> M bind(Function<T, ? implements Monad<?, U>> f);
    //public <T, V> M map(Function<T, V> f);
}
